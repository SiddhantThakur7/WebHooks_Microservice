<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Webhook Registry Home</title>
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <link rel="stylesheet" href="/css/admin.styles.css">
</head>

<body>
    <div class='full-page'>
        <div class="card">
            <h3 class="card-header text-center font-weight-bold text-uppercase py-4">
                Webhook Failures
            </h3>
            <div class="card-body">
                <div id="table" class="table-editable">

                    <span class="table-add float-right mb-3 mr-2">
                        <form action="/admin/register" method="get">
                            <button class="btn btn-info">Add</button>
                        </form>
                    </span>
                    <table class="table table-bordered table-responsive-md table-striped text-center">
                        <thead>
                            <tr>
                                <th class="text-center">Webhook #id</th>
                                <th class="text-center">Target URL</th>
                                <th class="text-center">Code</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for(i=0; i < codes.length; i++){ %>
                                <tr>
                                    <td class="pt-3-half" contenteditable="true"><%= hooks[i]._id %></td>
                                    <td class="pt-3-half" contenteditable="true"><%= hooks[i].target_url %></td>
                                    <td class="pt-3-half code" contenteditable="true"><%= codes[i] %></td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>

</html>